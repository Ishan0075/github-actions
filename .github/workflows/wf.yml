name: myfirst
#to push anything to this branch
on: 
  push:


jobs:
 job1:
   name: JOB1
   runs-on: ubuntu-slim
   steps:
     - name: step-1
       run: |
          pwd
          ls -lrt
          hostname
     - name: checkout
       uses: actions/checkout@v5.0.0
     - name: step-3
       run: |
         pwd
         ls -lrt
         hostname
      